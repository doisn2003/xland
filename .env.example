# Scaffold/demo currently needs no environment variables or service keys.
# Add documented variables here only when a feature actually consumes them.
# Never put server secrets in NEXT_PUBLIC_* variables.
